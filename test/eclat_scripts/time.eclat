# time
# 
# (time.eclat)
#

from programs.hike_default import hike_pass
from programs.misc import time
from loaders.basic import ip6_sc

# send all IPv6 packets to our chain
ip6_sc[ipv6_sc_map] = { (0): (print_example) }
ip6_sc.attach('DEVNAME', 'xdp')

def print_example():
    time()
    hike_pass()
    return 0
